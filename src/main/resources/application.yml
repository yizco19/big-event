spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/big_event
    username: root
    password: root

mybatis:
  configuration:
    map-underscore-to-camel-case: true #abrir la configuración de mybatis en el archivo mybatis-config

app:
  file:
    storage-path: D:/code/files/
